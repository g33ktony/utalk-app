import { ApolloClient, InMemoryCache, ApolloLink } from '@apollo/client'
import { mockServer } from '@graphql-tools/mock'
// import mockSchema from './mock/mock'

// Create a mock link with your schema
// const mockLink = new MockLink(mockSchema)

// Create a custom Apollo Link that combines the mock link and any other links you may need
// const link = ApolloLink.from([mockLink])

// Create the Apollo Client with the custom link and cache
// const client = new ApolloClient({
//   link,
//   cache: new InMemoryCache()
// })

// export default client
