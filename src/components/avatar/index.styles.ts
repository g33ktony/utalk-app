import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  authorRow: {
    alignItems: 'center',
    marginBottom: 8
  },
  avatar: {
    marginRight: 8
  },
  author: {
    fontSize: 16,
    color: 'white'
  },
  userAvatarContainer: {
    marginRight: 8
  }
})

export default styles
