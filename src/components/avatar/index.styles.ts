import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  authorRow: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 8
  },
  avatar: {
    borderRadius: 20,
    marginRight: 8
  },
  author: {
    fontSize: 16,
    color: 'white'
  }
})

export default styles
